apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nextjs-service.fullname" . }}-test-connection"
  labels:
    {{- include "nextjs-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nextjs-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
