server:
  ha:
    enabled: true
    replicas: 1

  raft:
    enabled: true
    path: /vault/data

  dataStorage:
    enabled: true
    size: 10Gi
    mountPath: /vault/data
    storageClass: standard
    accessMode: ReadWriteOnce
ui:
  enabled: true

service:
  enabled: true
  type: ClusterIP
